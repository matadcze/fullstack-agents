language: rust

tasks:
  build:
    command: cargo build --release -p rust-svc
    inputs:
      - 'src/**/*.rs'
      - 'Cargo.toml'

  test:
    command: cargo test -p rust-svc
    inputs:
      - 'src/**/*.rs'
      - 'Cargo.toml'
